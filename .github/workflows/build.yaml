name: banks2ledger

on:
  push:
    branches: [ main ]
    ignore_tags: "**"
  pull_request:
    branches: "**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run tests
        uses: tzafrirben/babashka-docker-action@v1.1
        run: ./test.sh
